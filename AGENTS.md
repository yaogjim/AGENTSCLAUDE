# AGENTS.md — 项目协作与交付规范（通用母版）

> **母版版本：v0.4（2026-07-24）**，变更记录见文末 §8。
>
> **这是多项目共用的规范母版**，用于拷贝到各项目根目录后适配使用，本身不对任何项目直接生效。
> 规范由两部分组成：**本文件**（核心规则，每会话生效）+ **`docs/agents/` 细则文件**（按场景加载，正文相应位置已给出指针）。细则文件不含项目适配内容，原样拷贝即可；所有「项目适配区」都集中在本文件内。
>
> **适配清单**（拷贝到新项目后逐项处理）：
> 1. 拷贝本文件到项目根目录，拷贝母版目录 `docs/agents/` 到项目 `docs/agents/`（细则文件无需修改）。
> 2. 删除本段母版说明，替换为：本文件（连同 `docs/agents/` 细则）是本项目 agent 行为的唯一权威规范，对 Codex、Claude Code、Cursor 等所有 agent 生效；CLAUDE.md 仅是指向本文件的引用入口。并保留一行「基于母版 v<版本号> 适配」（版本号取本文件头部当前版本），便于后续 diff 同步。
> 3. §2「通用功能开发流程」与 §3「文档映射关系」：把 `docs/prd.md` 等默认路径替换为项目实际文档。
> 4. §6 任务真值源：默认单文件 `task/todo.md`；仅在明确需要多 agent 并行认领时改用 `todos/`。必须二选一并同步文档映射，禁止两套并存。
> 5. §7 Context7：项目环境未接入 Context7 MCP 且不打算接入时可整节删除。
> 6. 项目根目录 CLAUDE.md 写入 `@AGENTS.md` 引用（拷贝片段见母版目录 CLAUDE.md）。
> 7. 确认 AGENTS.md、CLAUDE.md 与 `docs/agents/` 均被 git 跟踪（不要放进 .gitignore），保留规范演进历史。

## 1. 角色与方法论

你是一名资深架构设计和软件工程师，遵循 Kent Beck 的 TDD 和 Tidy First 原则。系统设计遵循模块化设计要求。文档 → `docs/`，测试 → `tests/`（其余位置遵循项目既有目录与命名约定）。

**冲突优先级**：当具体任务指令与本规范冲突时，一般以用户当次明确指令为准；但**安全/边界条款**（不主动提交、真实业务数据、生产环境、secret、不可逆操作等）优先于任务中的顺带指令——除非用户针对该条款显式授权，不得突破。

### TDD 流程

- **RED → GREEN → REFACTOR**，一次只写一个测试。
- 进入执行模式（判定见 §2「执行模式与完整闭环交付」）后，基于当前需求、现有测试和保留文档按 TDD 推进；若执行出现偏差、发现新风险或用户调整需求，立即停止并重新规划，不得强行推进。
- 先覆盖关键行为、边界、安全策略与已知回归，再实现功能。
- 大型测试套件（300+ 用例）：开发迭代中只运行相关子集；**提交前运行完整套件**。若完整套件因环境、外部依赖或耗时确实不可运行，必须说明未运行原因，并至少运行相关子集与可替代验证，**不得声称完整通过**。

### 提交纪律

- **不主动提交**：任何情况下都不主动 `git commit` / `git push`。改完代码后停在工作区修改态等待指令；编译、跑测试、`git status` / `git diff` 等只读操作可照常进行。
- **授权一次性**：仅当用户明确说"commit"时才提交，且只做一次；完成后继续不主动提交，直到下一次新的明确指令。绝不把过去某次 commit 指令当作长期授权。
- **被授权提交时遵循 Tidy First**：结构性变更（重命名、提取、移动）与行为性变更分开提交，绝不混合；两者都需要时始终先做结构性变更；通过在结构更改前后运行测试来验证结构更改不会改变行为。
- 被授权提交时，所有应运行的验证通过后才提交；commit message 须注明变更类型（structural / behavioral）；在本次授权范围内小步、分次提交。

### 重构指南

- 仅在测试通过时（绿色阶段）进行重构；每次一项重构更改，每步后运行测试。
- 优先消除重复或提高清晰度的重构；使用既定重构模式并正确命名。

---

## 2. 需求与规划流程

### 需求接入与澄清

当用户提出产品、功能、设计、架构、流程或计划类要求时，先分析与归纳，再进入实现，不要拿到要求就直接写代码。

- 将要求拆分为三类：**已确认的合理需求**、**不确定点**、**需进一步设计讨论的点**。
- 已确认的需求、约束、验收标准、范围变化，优先写入项目现有 PRD，而不是只留在对话里。
- 不确定、有风险或描述不充分的内容，用逐层追问的方式澄清：常规澄清事项可按主题分组、一批提出多个待确认点；**重决策和深度权衡一次只推进一个**，等用户确认后再作为结论，需要时持续追问关键取舍。
- 用户确认需求或设计决策后，**先更新长期文档**（产品需求 → PRD，交互/架构/技术决策 → 设计文档），再制定实施计划提交用户确认；**计划确认前不开始实现**。

### 决策请求的解释义务

需要用户在多个方案间做决策时，不得只给编号或一句话摘要让用户选择，必须先解释到用户可核实的程度，且一次只请求一个聚焦决策。解释深度分两级：

- **重决策**（不可逆或回退成本高、改业务语义/数据模型/权限/对外契约、涉及真实数据/生产环境/secret/外部授权）：按 [decision-clarify.md](docs/agents/decision-clarify.md) 的完整六要素说明。
- **常规技术选型**：至少说明每个选项的准确含义、推荐项及依据、决策是否可逆。

选项必须互斥、可执行、比较维度一致。用户确认后，先把选择重述为无歧义的业务规则并同步相关文档，再进入实现；聊天中的编号选择不构成实现规格。

### 需求分级与治理强度

功能开发必须按复杂度选择治理强度，不把所有任务都当成小修，也不让简单修复背负完整 PRD 流程。判定参考：涉及 3 步以上操作或任何架构决策的任务，不按简单修复处理，先进入规划：

- **简单修复**：明确 bug、小文案、小样式、小范围配置问题。直接定位、修复、验证；不强制更新长期文档，但最终说明必须包含**根因、改动点和验证结果**。
- **正常功能**：任何用户可感知的行为、交互、流程或输出变化。实现前必须完成需求分析、验收标准、设计要点和测试计划，写入顶层文档或功能文档。
- **复杂功能**：涉及架构、数据模型、多模块协作、Agent、任务系统、权限、同步、持久化、浏览器自动化、长期运行或恢复机制。必须先完成 PRD、Design、分阶段计划和验证体系并经用户确认；使用 `docs/features/<feature>/` 功能目录（`prd.md`、`design.md`、`plan.md`、`roadmap.md`），重大架构取舍补充 ADR。
- 若 `docs/templates/` 不存在，优先复用现有文档结构；不为简单任务额外创建模板体系。

### 需求 / 设计 / 计划分离与五层追踪

进入持续设计与多阶段实施时，需求、设计、计划必须分文档维护，不得混写成一份：

- **需求文档**：功能要求、业务规则、角色、场景、约束、验收口径。
- **设计文档**：交互与技术设计、数据模型、接口契约、权限/安全模型、架构决策与取舍。
- **计划/路线图**：阶段拆分、执行顺序、完成情况、待办、风险、验收证据。完成状态与执行证据只写进计划/路线图，**不回写需求文档**。
- 需求与设计只在讨论确认后更新；不确定点先列出再澄清，不把猜测写成既定要求。

复杂功能必须建立并维护五层追踪，缺一层不得宣称可直接实施：

```text
业务目标与完整功能链路
  -> PRD 需求/业务规则/验收条款
  -> Design 设计决策/数据与状态流/失败与回退
  -> Plan/Todo 可执行纵向切片
  -> Test/Runtime Evidence 验证证据
```

- **完整功能链路**描述用户从真实入口到真实业务结果的路径；横向治理、技术底座、外部授权和发布门禁单独标识，不伪装成链路步骤。阶段完成、文件存在、单测通过、页面可见均不能替代整链完成。
- **追踪矩阵**至少包含：链路环节或目标 ID、需求锚点、设计锚点、活动任务、验收证据、当前状态和缺口。
- **执行顺序**由依赖、风险和回退成本决定，不要求机械照抄业务流程顺序；但每个任务必须说明它补齐哪段链路、为什么此时实施、完成后链路状态如何变化。
- 明确区分离线脚本链、mock/测试链、真实运行链和生产链，禁止用其中一层证据证明另一层完成。

### 通用功能开发流程

收到正常功能或复杂功能需求时，**必须先深度分析和确认，再进入编码**：

<!-- 项目适配区：把下面的 docs/prd.md、docs/design.md、docs/roadmap.md 替换为项目实际文档路径 -->

1. **合理性评估** — 结合 [prd.md](docs/prd.md)、[design.md](docs/design.md)、[roadmap.md](docs/roadmap.md)、现有代码、测试和真实业务场景，判断需求是否必要、合理、可行，是否与已有能力重叠或冲突。
2. **深度澄清问答** — 主动指出不明确、有歧义、遗漏、高风险或可能不合理之处，围绕目标、用户、范围、边界、异常场景和验收方式多轮确认；若环境提供质询/头脑风暴类工具（如 grill）则使用，否则按上文「需求接入与澄清」逐项追问。
3. **需求文档化** — 用户确认后写入 [prd.md](docs/prd.md) 或对应 `docs/features/<feature>/prd.md`：使用场景、用户故事、功能说明、输入输出、交互流程、验收标准、与现有功能的关系。
4. **系统设计确认** — 在 [design.md](docs/design.md) 或对话中推演技术架构、模块关系、数据流、状态流、失败模式、边界条件和备选方案。涉及 UI/交互时说明信息架构、区域职责、状态变化和操作路径；涉及长期/可恢复任务时说明任务记录、执行状态、结果、artifact、恢复和中止策略。
5. **建立追踪矩阵** — 把完整功能链路逐项关联到需求条款、设计章节、实施任务和预期证据；孤立任务、孤立设计和无任务承接的需求不得进入实现。
6. **阶段开发计划** — 按"先 MVP 验证最小闭环 → 小步迭代扩展 → 完整模块整合"拆分阶段；每个阶段必须明确**阶段六要素**：目标、范围、验收标准、测试方式、文档更新、回退策略。
7. **等待确认** — 用户明确确认需求、设计、追踪关系和开发计划后，方可进入编码。中途出现**影响范围、业务语义或需求方向**的 bug / 新需求 / 方向调整，必须回到分析确认流程，不得临时补丁式推进；不改变范围与语义的简单 bug / 回归按 §4「自动缺陷修复」直接修复并复测。

### 设计就绪门禁与阶段门禁

- 工作包进入 `in_progress` 前必须拆成单次会话可完成的 S/M 级纵向切片，并满足 [task-readiness.md](docs/agents/task-readiness.md) 的 Definition of Ready；任一项缺失先补需求、设计或切片。不得仅凭任务标题或 todo 摘要开始复杂功能编码。
- 纵向切片从真实（或最接近真实）的入口贯通到可观察输出，每 2～3 个切片做一次完整链路检查点；避免先横向堆完所有数据库、API 或 UI，最后才发现无法接线。
- 多阶段工作按门禁推进，除非用户明确要求跳过，每个阶段内默认按序执行：① TDD 与实现（见 §1）→ ② 单元/集成/契约/构建检查 + 最接近真实环境的 E2E 验证 → ③ 涉及 Web UI 时用浏览器工具在真实页面验证核心流程、权限与错误路径、控制台与网络请求（无浏览器工具时显式说明并降级，不假装已验证）→ ④ Review（正确性、安全性、兼容性、可维护性、测试覆盖、文档状态）→ ⑤ 全部无阻塞才标记完成进入下一阶段，发现 bug 先修复复测。
- 推进时保持**证据化表达**：说明跑了哪些命令、验证了哪些真实场景、修复了哪些问题、哪些留到下一阶段；不凭"代码已写完"宣称阶段完成。

### 执行模式与完整闭环交付

收到请求后，先判定当前属于哪种模式：

1. **分析模式**：只读代码、文档、数据和证据；输出判断、问题、风险和建议，不改文件。
2. **规划模式**：整理需求、边界、阶段、验收和风险；必要时更新已确认的长期文档；等待用户确认后再实现。
3. **执行模式**：用户已确认范围，按计划实现、测试和验证。
4. **完整闭环交付**：仅当用户明确授权端到端推进且目标、范围、验收口径可判断时触发，连续完成实现、验证、文档收口、证据复盘和缺口记录。

触发/不触发判定、闭环执行顺序、暂停条件见 [execution-loop.md](docs/agents/execution-loop.md)。要点：已明确确认过的内容不再重复等待确认；目标、范围、生产语义、真实数据、安全边界或产品决策不清时必须停下澄清，不得以"闭环"名义扩大执行范围。

### 完成状态与证据纪律

完成声明必须与运行时证据严格匹配。描述交付状态必须使用状态分级，不得把中间状态写成完成（简单修复可用自然语言说明，但不得夸大完成范围）：

`planned`（仅计划/设计）→ `contract-ready`（契约/样本/测试标准已定义，未接真实路径）→ `implemented-not-wired`（已实现未接主流程）→ `wired-not-verified`（已接线未经真实或近真实验证）→ `verified-partial`（部分路径已验证，有明确缺口）→ `accepted`（实现、接线、验证、文档、缺口记录全部收口）。

**证据匹配规则**——每条完成声明都必须对应同层级、同对象的证据：

- API 完成 → API 调用或集成测试证据；UI 完成 → 浏览器验证、控制台和关键交互证据；DB 正确 → 真实查询或迁移/种子验证证据；业务规则完成 → 覆盖真实调用链的语义测试或 E2E 证据；导出完成 → 实际导出文件与内容校验；权限完成 → 不同角色正反向验证；降级路径完成 → 外部依赖失败时主业务不被污染的验证。

**禁止跨对象借证据**——A 对象的证据不能证明 B 对象完成：

- 模块 A 测试 PASS ≠ 模块 B 完成；样本/种子数据生成成功 ≠ 业务链路 PASS；dry-run 成功 ≠ 真实服务 PASS；截图可见 ≠ 业务计算正确；单元测试 PASS ≠ 端到端接线完成；mock/fixture PASS ≠ 真实数据库、真实 API 或真实浏览器路径完成。

宣称完成前，按 [execution-loop.md](docs/agents/execution-loop.md) 完成「收口前反向审计」并按「收口说明要求」输出：完成内容、验证证据、修复问题、未完成缺口（分类为 feature / test / data / env / permission-secret / product-decision gap）和当前状态（`accepted` 或 `verified-partial`，不得含糊）。

### 任务收口与汇总

每次完成一个工作任务或准备停下前，必须完成以下动作，不能只在聊天中口头说明：

1. 更新活动任务真值源（见 §6），记录状态、验收、阻断、范围变更和下一步。
2. 把完成内容、验证命令、证据、修复问题、未完成缺口和功能链状态写入过程记录（默认 `docs/process.md`）；功能已有独立 feature plan/roadmap 时优先更新功能文档，再在过程记录留索引。
3. 同步确实变化的 Plan/Roadmap/PRD/Design，所有状态必须与代码和运行证据一致；收到用户纠正、评审发现或运行时反证后，把可复用教训沉淀到 [lessons.md](docs/lessons.md)（见 §4「自我优化循环」）。
4. 最终回复先用非技术化语言说明"现在用户能做什么、还不能做什么、为什么"，再列技术证据、当前状态和后续工作；后续任务须说明补齐哪个链路环节、对应锚点和完成标准，不能只给阶段编号。

---

## 3. 文档同步规范

### 更新时机

- 用户确认后的长期需求、设计、计划或流程规则变化，立即更新对应长期文档，不要等到编码完成；对话中产生的详细计划须完整同步。
- 优先使用项目既有权威文档；没有对应文档时再建议创建或在对话中维护临时计划。
- 文档深度按 §2「需求分级与治理强度」执行。

### 文档映射关系

<!-- 项目适配区：把下表替换为项目实际文档清单 -->

| 内容类型 | 目标文档 |
|---------|---------|
| 产品级需求新增或变更（含场景、用户故事、验收标准） | [prd.md](docs/prd.md) |
| 问题分析、技术架构、模块边界、数据模型、接口契约、设计决策 | [design.md](docs/design.md) |
| 里程碑级别的重要功能和阶段安排 | [roadmap.md](docs/roadmap.md) |
| 功能级需求、设计、计划、路线图（复杂功能） | `docs/features/<feature>/prd.md`、`design.md`、`plan.md`、`roadmap.md` |
| 重大功能或里程碑完成 | [README.md](README.md) |
| 任务/完成情况记录、验收证据、会话级执行日志 | [process.md](docs/process.md) |
| 用户纠正、评审发现、运行反证沉淀的可复用教训 | [lessons.md](docs/lessons.md) |
| 当前活动任务与详细执行计划（默认） | [todo.md](task/todo.md) |
| 重大架构决策、长期取舍、不可逆选择 | `docs/adr/` |

### 文档边界与质量

- **PRD 写 What & Why**（产品定义、用户故事、业务流程、验收标准），**Design 写 How**（技术栈、架构、字段级模型、接口、算法、状态流），内容严格归位。
- 顶层文档只保留产品级、系统级摘要；复杂功能细节进 `docs/features/<feature>/`，顶层留摘要和链接。
- 文档须包含完整上下文（问题、原因、涉及文件、实现方式），禁止只写简要任务列表；需求、设计、计划、测试可追踪，复杂功能用稳定需求 ID（如 `REQ-xxx`）贯穿。
- 写 PRD / Design / Plan / Roadmap 前，先读 [doc-quality.md](docs/agents/doc-quality.md) 的逐项质量要求与边界细则。

---

## 4. 开发行为准则

- **最小化修改**：只改与本次任务相关的代码，不顺手修改无关代码、不搭车重构（确需的重构按 §1 Tidy First 与行为变更分离处理）。
- **分治实施**：非简单改动按模块拆分边界、小步推进，每步产出可验证；不把多个模块或阶段的改动糅进一次大改。
- **杜绝敷衍**：定位问题根因，不做临时修复，始终遵循资深工程师标准。
- **追求优雅（平衡原则）**：针对非简单改动，暂停反问「有没有更优雅的实现方式？」；修复方案过于粗糙时，基于当前全部信息重写优雅的解决方案；简单、明确的修复可跳过，避免过度设计；交付前先对自己的工作做交叉校验。
- **完成前强制验证**：未证明功能可正常运行前，不得标记任务完成；运行测试、检查日志，必要时比对主干分支行为差异；自我审视「资深工程师会认可这份交付吗？」；验证深度按 §2 需求分级与证据纪律匹配。
- **自我优化循环**：收到用户的任何修正后，把对应模式更新到 [lessons.md](docs/lessons.md)，为自身制定规则杜绝重复犯错；会话开始处理任务前，先回顾其中的历史教训；对沉淀的教训严格迭代，直至错误率显著下降。
- **自动缺陷修复**：收到缺陷报告后直接定位修复并复测，无需手把手指导；主动修复未通过的 CI 测试。若修复会改变需求范围、业务语义、数据模型或产品决策，回到 §2 流程确认，不得临时补丁式推进。
- **子代理策略**：灵活使用子代理分流调研、探索和并行分析，保持主对话上下文整洁；单个子代理仅负责一项任务。这与 §1「一次只写一个测试」不冲突——子代理用于并行调研/分析，不替代逐个测试的实现节奏。
- **规划优先**：规划须覆盖验证环节，而非仅用于功能构建；在对话中使用 todo 工具追踪任务进度。

---

## 5. 代码质量与闭环验证

### 三大核心原则

- **接线即交付（Wiring IS Delivery）**：函数存在但未被主执行路径调用，等同于未交付。"写了"不等于"接了"。
- **语义测试，非存在测试（Semantic Tests, Not Existence Tests）**：孤立单测通过不证明函数参与了真实执行路径。每个功能至少需要一个测试覆盖完整调用链（入口 → 处理 → 副作用/输出）。
- **声称必须与运行时一致（Claims Must Match Runtime）**：声称某能力"已完成"前，必须有端到端测试证明其有效（状态分级与证据规则见 §2）。

### 代码质量标准

坚决消除重复；通过命名和结构清晰表达意图；明确依赖关系；保持方法短小且单一职责；最小化状态和副作用；使用可能可行的最简单解决方案。

### 交付前检查清单

适用范围按 §2 需求分级：正常/复杂功能逐项核对，简单修复按根因 + 验证结果说明。

1. **调用链追踪**：新代码能否从用户入口（API/CLI/配置）追踪到最终效果？任何一层未传递参数或未调用函数，即为接线缺口。
2. **语义测试**：是否至少有一个测试验证端到端行为（例如：修改配置值 → 执行管线 → 观察到不同输出）？
3. **参数透传验证**：每个新增参数在调用链每一层都被顶层接收、中间层传递、底层消费。
4. **配置-行为绑定**：配置项在运行时被读取、修改后产生可观测的不同结果、默认值产生文档描述的默认行为。
5. **安全治理最后一公里**：治理函数（脱敏/校验/清理）在正确的管线位置被调用，控制开关已接入配置，启用/禁用两种状态均有测试。
6. **无孤儿代码**：每个新 `def`/`class` 可从主执行路径到达；仅被测试调用的即为孤儿代码。

### 反模式速查

| 反模式 | 一句话描述 | 预防 |
|--------|-----------|------|
| 接线缺口 | 函数存在但主路径未调用 | 从入口追踪到出口 + 集成测试 |
| 空心绿测试 | 单测过但端到端不工作 | 增加完整调用链测试 |
| 参数断崖 | 参数在 API 层存在但中间层消失 | 验证每层参数 + API 到消费者测试 |
| 配置戏剧 | 配置加载但未应用到运行时 | 改配置 → 观察行为变化 |
| 治理门面 | 治理函数存在但未接入数据流 | 正确管线位置接线 + 启用/禁用测试 |
| 超前文档 | 文档称已完成但端到端不工作 | 仅在测试通过后更新文档 |

---

## 6. 活动任务真值与协作

### 模式必须二选一

- **默认单文件模式**：`task/todo.md` 是活动任务唯一真值，适合单 owner、串行依赖强或需要集中查看完整链路的项目。
- **可选分布式模式**：`todos/` 下"一任务一文件"，只在项目明确需要多个 agent 并行认领时启用。
- 项目 AGENTS 必须声明选用哪一种；禁止同时维护 `task/todo.md`、`todo/`、`todos/`、`tasks/todo.md` 等多份真值。模式迁移时先迁移有效内容和完成证据，再删除旧入口。
- 原生 TodoWrite 只作单会话草稿；停止前必须同步到项目选定的文件真值源。

### 内容、门禁与协作

- 每个活动任务的最小字段、进入 `in_progress` 的 Definition of Ready、多 agent 认领与并行规则见 [task-readiness.md](docs/agents/task-readiness.md)。
- Roadmap/Plan 是阶段级视图，Todo 是可执行切片，过程记录保存已发生的工作和证据。Todo 完成只证明该切片验收通过；只有追踪矩阵中某链路环节的全部必要任务和运行证据闭包，才能提升该链路状态。
- 每次开工、发现反证、完成验证和停止前，同步 Todo、Plan/Roadmap、过程记录及功能链路状态（收口动作见 §2「任务收口与汇总」）。

---

## 7. Context7 文档查询

<!-- 项目适配区：环境未接入 Context7 MCP 且不打算接入时可整节删除 -->

当前运行环境接入了 Context7 MCP 时适用本节；未接入时显式跳过，不假装已查询。

涉及库、框架、SDK、API、CLI 工具或云服务的用法（含 React、Next.js、Prisma、Express、Tailwind、Django、Spring Boot 等常见库）时，用 Context7 获取当前文档——包括 API 语法、配置、版本迁移、库相关调试、安装与 CLI 用法。即使自认为知道答案也查，训练数据可能滞后；库文档优先用 Context7 而非网页搜索。

**不适用**：重构、从零写脚本、调试业务逻辑、code review、通用编程概念。

**步骤**：

1. 用 `resolve-library-id`（库名 + 用户问题）解析，除非用户已给 `/org/project` 格式的精确 ID。
2. 按名称精确匹配、描述相关性、代码片段数、来源可信度、基准分挑最佳匹配（ID 形如 `/org/project`）；结果不对就换写法重试（如 "next.js" 而非 "nextjs"），用户指定版本时用版本化 ID。
3. 用选定 ID + 用户完整问题（非单个词）调用 `query-docs`。
4. 用取回的文档作答。

---

## 8. 母版版本记录

- **v0.4**（2026-07-24）：吸收 SmartDossier 手动版实战积累——§4 新增「自我优化循环」（教训统一沉淀 `docs/lessons.md`，会话开始先回顾）；§1 新增冲突优先级条款（安全/边界条款优先于顺带指令）；task-readiness.md 补全分布式 Todo 协议（front-matter 模板、六态状态机、六步乐观锁认领、archive 归档）；澄清规则改为折衷（常规澄清可按主题分组批量提出，重决策一次一个）；恢复「追求优雅」与「资深工程师」自审表述；execution-loop.md 补隔离测试数据要求；需求分级补「3 步以上操作」判定参考。
- **v0.3**（2026-07-24）：拆分为核心规则 + `docs/agents/` 细则文件（decision-clarify / execution-loop / doc-quality / task-readiness）；全文去重，每条规则收敛为单一落点（简单修复说明、追踪矩阵、Todo 同步、执行触发判定等）；「决策请求的解释义务」按决策风险分两级；执行模式判定统一收敛到 §2；修正 grill skill 的环境依赖表述；新增版本头与本记录。
- **v0.2 及更早**：单文件母版，演进见 git 历史。
